
trigger:
  branches:
    include:
      - feature/dev/101
pool:
  name: Jagseer-pc
  
variables:
  TF_VAR_environment: dev
  
stages:
  - stage: Terraform_installer_Initplan
    displayName: Terraform installInit Plan
    jobs:
      - job: Terraforminitplan
        displayName: Terraform init and Plan
        steps:
        - task: TerraformInstaller@1
          inputs:
            terraformVersion: 'latest'
        - task: TerraformTask@5
          env:
            TF_VAR_environment: $(TF_VAR_environment)
          inputs:
            provider: 'azurerm'
            command: 'init'
            workingDirectory: '$(System.DefaultWorkingDirectory)/Environment/Dev'
            backendServiceArm: 'Sp1'
            backendAzureRmOverrideSubscriptionID: '68091150-801e-4498-8be2-e5d4bcf9919c'
            backendAzureRmResourceGroupName: 'imigrant-help'
            backendAzureRmStorageAccountName: 'jagseerstg123'
            backendAzureRmContainerName: 'containertf'
            backendAzureRmKey: 'dev.terraform.tfstate'

        
        - task: TerraformTask@5
          env:
            TF_VAR_environment: $(TF_VAR_environment)
          inputs:
            provider: 'azurerm'
            command: 'plan'
            workingDirectory: '$(System.DefaultWorkingDirectory)/Environment/Dev'
            environmentServiceNameAzureRM: 'Sp1'
            environmentAzureRmOverrideSubscriptionID: '68091150-801e-4498-8be2-e5d4bcf9919c'
        - task: TerraformTask@5
          inputs:
            provider: 'azurerm'
            command: 'custom'
            workingDirectory: '$(System.DefaultWorkingDirectory)/Environment/Dev'
            outputTo: 'console'
            customCommand: 'fmt'
            environmentServiceNameAzureRM: 'Sp1'
            environmentAzureRmOverrideSubscriptionID: '68091150-801e-4498-8be2-e5d4bcf9919c'

      - job: tfsec
        displayName: tfsec scan
        dependsOn: Terraforminitplan
        steps:
        - task: CmdLine@2
          inputs:
            script: 'which brew || /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"'
            workingDirectory: '$(System.DefaultWorkingDirectory)/Environment/Dev'
        - task: CmdLine@2
          continueOnError: true
          inputs:
            script: |
              brew install tfsec
              tfsec .
            workingDirectory: '$(System.DefaultWorkingDirectory)/Environment/Dev'

      - job: tflint
        displayName: tflint
        dependsOn: tfsec
        steps:
        - task: CmdLine@2
          continueOnError: true
          inputs:
            script: |
              brew install tflint
              tflint --version
            workingDirectory: '$(System.DefaultWorkingDirectory)/Environment/Dev'
